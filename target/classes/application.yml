spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/bfi
    username: postgres
    password: sourour02
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  servlet:   # to limit file/photo upload size
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB




