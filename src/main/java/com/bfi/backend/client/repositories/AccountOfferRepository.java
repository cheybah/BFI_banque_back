package com.bfi.backend.client.repositories;

import com.bfi.backend.client.entites.AccountOffer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccountOfferRepository extends JpaRepository<AccountOffer, Long> {
}
